import { describe, test, expect } from "vitest";

describe("MoveUser UseCase", () => {
  test("Given a user trying to move to the left without being blocked, should move", () => {
    expect(true).toBe(true);
  });
});
